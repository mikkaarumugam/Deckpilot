"""
Unit tests for the parser. Wired up in M3.

The eval harness (tests/eval.py) covers end-to-end accuracy/latency on 20 NL
prompts. This file is for fast, deterministic tests of individual parser rules.
"""

from __future__ import annotations

# TODO(M3): add tests once parser.parse() exists.
